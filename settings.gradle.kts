pluginManagement {
    repositories {
        maven {
            name = "Fabric"
            url = uri("https://maven.fabricmc.net/")
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

// include(":gravity-api")
// project(":gravity-api").projectDir = File("./gravity-api")
